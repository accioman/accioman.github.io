Salva qui la foto profilo usata dal blocco LinkedIn del sito.

Percorso previsto di default:
- linkedin-profile.jpg

Con il file presente, `scripts/build-pages.ps1` lo includera automaticamente nella build `.site`.
